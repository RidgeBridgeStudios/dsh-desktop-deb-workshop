# Always run before committing

- Test command: `npm test` (ensures all 185 tests pass across unit and integration suites).
- Package build verification: `./build.sh` (ensures Debian package staging, permission setting, and deb creation succeed).
